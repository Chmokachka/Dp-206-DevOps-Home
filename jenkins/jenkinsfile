pipeline {
    agent {label "jenkins_slave"}
    stages {
        stage ("Sonar"){
        steps{
          script{
            sh "mvn --version"
            sh "mvn clean install"
              }
          }
        }
      }
}
