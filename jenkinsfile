pipeline {
    agent {
        label 'master'
    }
    tools{
      maven 'Maven'
    }
    stages {
        stage('Build') {
            steps {
              sh 'mvn -v'
            }
        }
    }
}
