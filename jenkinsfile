pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script{
                  sh 'pwd'
                  sh "ls -la"
                }
              withMaven(maven: 'Maven'){
                sh "sudo mvn clean install"
              }
            }
        }
    }
}
