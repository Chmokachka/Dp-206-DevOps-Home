pipeline {
    agent {label "jenkins_slave"}
    stages {
        stage('Build') {
            steps {
                script{
                  sh 'pwd'
                  sh "ls -la"
                  sh "java -v"
                }
              withMaven(maven: 'Maven'){
                sh "mvn clean install"
              }
            }
        }
    }
}
