pipeline {
    agent {label "jenkins_slave"}
    stages {
        stage('Build') {
            steps {
                script{
                  sh 'pwd'
                  sh "ls -la"
                }
              withMaven(maven: 'Maven'){
                sh "mvn --version"
                sh "mvn clean install -DskipTests"
              }
            }
        }
    }
}
