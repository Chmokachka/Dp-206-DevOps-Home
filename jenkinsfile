pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script{
                  sh 'pwd'
                  sh "ls -la"
                }
              withMaven(maven: 'Maven'){
                sh "mvn --version"
                sh "mvn clean install"
              }
            }
        }
    }
}
