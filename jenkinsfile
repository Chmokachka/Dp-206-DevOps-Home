pipeline {
    agent {jenkins slave}
    stages {
        stage('Build') {
            steps {
                script{
                  sh 'pwd'
                  sh "ls -la"
                }
              withMaven(maven: 'Maven'){
                sh "mvn clean install"
              }
            }
        }
    }
}
